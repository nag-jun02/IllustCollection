
<div class="div_wrapper">
      <%#<ul class="ul_Wrap">
        <% @users.each do |user| %>
          <%#<li class="li_ImageW">
            <%= link_to user_path(user) do %>
              <%#<% if user.avatar? %>  <!-- アップロード画像がある場合に実行する -->
                <%#<%= image_tag user.avatar.url, style: "width: 50px; height: 50px;", class: "rounded-circle" %><!-- userインスタンスの画像ファイルのURLを取得し表示 -->
              <%#<% end %>
            <%#<% end %>
          <%#</li>
        <% end %>
      <%#</ul>%>

      <div style="color: white; font-size: 30px;">最新</div>
      <ul class="ul_Wrap">
        <% @newillusts.each do |illust| %>
          <li class="li_ImageW">
            <div class="ImageUser">
              <%= link_to illust_path(illust), data: {"turbolinks" => false} do %>
                <% if illust.image? %>  <!-- アップロード画像がある場合に実行する -->
                  <%= image_tag illust.image.url, class: "IMGIllust", width: "160", height: "160" %><!-- userインスタンスの画像ファイルのURLを取得し表示 -->
                <% end %>
              <% end %>
            </div>

            <div class="Illust_index">
              <%= illust.title %>
            </div>
 
          </li>
        <% end %>
      </ul>
</div>